export default function DashboardPage() {
  return (
    <main style={{ fontFamily: 'system-ui', padding: '2rem', maxWidth: 480, margin: '0 auto' }}>
      <h1>Budget</h1>
      <p>
        Deploy pipeline is live. Real balance/spend/budget widgets replace this
        page next — see README.md, step 8.
      </p>
      <p>
        <a href="/api/health">Check database connection →</a>
      </p>
    </main>
  );
}
